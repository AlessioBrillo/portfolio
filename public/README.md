# public/

Static assets served as-is from the site root.

- `favicon.svg` — the minimal brand mark (an orange ascent arrow / abstract wing).
- **OG image (to add):** a curated `og-image.png` (1200x630) for link previews —
  Night background, name in Fraunces, orange thread, one line. See
  `docs/content/personalization.md`. Reference it from `index.html` via
  `<meta property="og:image" ...>` once created.
